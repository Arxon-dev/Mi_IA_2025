version: "2"
authtoken: # Tu token de ngrok aquí

tunnels:
  web:
    addr: 3001
    proto: http
    # bind_tls: true
  
  # Si necesitas más túneles, descomenta y configura:
  # api:
  #   addr: 3001
  #   proto: http
  
  # webhook:
  #   addr: 8080
  #   proto: http